version: "3.9"

services:
  redis:
    image: redis:latest
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - app_net

volumes:
  redis-data:

networks:
  app_net:
    driver: bridge
